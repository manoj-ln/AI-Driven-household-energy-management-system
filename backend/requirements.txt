fastapi>=0.110.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
joblib==1.3.2
numpy>=2.4.4
pytest==7.4.0
# anyio provides the @pytest.mark.anyio marker that test_integration.py relies on.
# anyio 3.x's trio backend references trio.MultiError, which was removed in
# trio >= 0.32. Pinning anyio 4.x (which uses BaseExceptionGroup directly) and
# a compatible trio ensures the marker runs on the asyncio backend without error.
anyio>=4.0.0,<5.0.0
trio>=0.22.0,<0.32.0
pandas>=1.5.0
scikit-learn>=1.3.0
xgboost>=1.7.0
lightgbm>=3.3.0
matplotlib>=3.6.0
sqlalchemy>=2.0.0
alembic>=1.12.0
python-multipart>=0.0.6
httpx>=0.27.0
langdetect>=1.0.9
# Optional real-time link; the app runs without a broker (MQTT disabled by default).
paho-mqtt>=2.1.0
