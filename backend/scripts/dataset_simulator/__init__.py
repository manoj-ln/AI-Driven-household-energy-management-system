"""Per-minute synthetic household energy dataset simulator.

Generates enterprise-grade synthetic smart-home energy datasets with
realistic Indian household behaviour, weather, occupancy, events, and
anomalies. Output is a wide CSV: one row per minute, one column per
metered device, plus rich metadata columns.
"""
